Canonical Prisma schema lives in `backend/prisma/schema.prisma`.

Migrations and seeds are maintained in `backend/prisma`.
Use root-level scripts (see package.json) to run Prisma commands with the backend schema.
